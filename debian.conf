#!/bin/bash

# ---------------------------------------------------------
INPUT="`pwd`"
PACKAGE="bibtexconv"
CONFOPT=""
INPUT_DISTRIBUTION=natty
MAKE_DIST="./bootstrap && ./configure $CONFOPT && make dist"
MAINTAINER="Thomas Dreibholz <dreibh@iem.uni-due.de>"
# ---------------------------------------------------------
